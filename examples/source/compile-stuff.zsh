# this will re-compile all teh functions every time your shell starts.
# you probably do NOT want this, and is just here as an example/reminder that compiling is a thing, and you might want to do it occasionally

autoload toasty-zsh

toasty-zsh -cl

# vim: ft=zsh
